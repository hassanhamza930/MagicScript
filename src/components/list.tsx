import { CourseCanvasList } from "@/interfaces";

function CourseCanvasList(props: CourseCanvasList) {
  return (
    <div className="relative h-full w-96 flex justify-start items-start flex-none bg-white/90 rounded-md"></div>
  );
}

export default CourseCanvasList;
